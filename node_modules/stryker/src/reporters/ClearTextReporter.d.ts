import { Reporter, MutantResult } from 'stryker-api/report';
export default class ClearTextReporter implements Reporter {
    private out;
    private writeLine(output?);
    onAllMutantsTested(mutantResults: MutantResult[]): void;
    private logMutantResult(result, logImplementation);
    private getColorForMutationScore(score);
}
