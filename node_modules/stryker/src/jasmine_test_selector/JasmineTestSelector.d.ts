import { TestSelector, TestSelectorSettings } from 'stryker-api/test_selector';
export default class JasmineTestSelector implements TestSelector {
    private settings;
    constructor(settings: TestSelectorSettings);
    select(ids: number[]): string;
}
