/// <reference types="es6-promise" />
import { Config } from 'stryker-api/config';
import { StrykerOptions } from 'stryker-api/core';
import { MutantResult } from 'stryker-api/report';
import './jasmine_test_selector/JasmineTestSelector';
export default class Stryker {
    config: Config;
    private reporter;
    private testSelector;
    constructor(options: StrykerOptions);
    runMutationTest(): Promise<MutantResult[]>;
    private filterOutUnsuccesfulResults(runResults);
    private loadPlugins();
    private initialTestRun(inputFiles);
    private generateAndRunMutations(inputFiles, initialRunResults, testRunnerOrchestrator);
    private generateMutants(inputFiles, runResults);
    private wrapUpReporter();
    private applyConfigWriters();
    private freezeConfig();
    private logInitialTestRunSucceeded(runResults);
    private setGlobalLogLevel();
    private logFailedTests(unsuccessfulTests);
}
