import Mutant from './Mutant';
import { RunResult } from 'stryker-api/test_runner';
export default class MutantRunResultMatcher {
    private mutants;
    private runResultsByTestId;
    constructor(mutants: Mutant[], runResultsByTestId: RunResult[]);
    matchWithMutants(): void;
    private findSmallestCoveringStatement(mutant, coveredFile);
    private isNewSmallestStatement(originalLocation, newLocation);
    private statementCoversMutant(mutant, location);
}
