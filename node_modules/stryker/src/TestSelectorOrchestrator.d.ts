import { TestSelector } from 'stryker-api/test_selector';
import { StrykerOptions } from 'stryker-api/core';
export default class TestSelectorOrchestrator {
    private options;
    constructor(options: StrykerOptions);
    determineTestSelector(): TestSelector;
    private determineTestSelectorBasedOnTestSelectorSetting();
    private determineTestSelectorBasedOnTestFrameworkSetting();
    private informAboutKnownTestSelectors();
    private createTestSelector(name);
    private testSelectorExists(maybeSelector);
    private createSettings();
}
