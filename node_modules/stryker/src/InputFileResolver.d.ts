/// <reference types="es6-promise" />
import { InputFile, InputFileDescriptor } from 'stryker-api/core';
export default class InputFileResolver {
    private inputFileDescriptors;
    private mutateFileExpressions;
    constructor(mutate: string[], allFileExpressions: Array<InputFileDescriptor | string>);
    resolve(): Promise<InputFile[]>;
    private static markAdditionalFilesToMutate(allInputFiles, additionalMutateFiles);
    private static warnAboutNoFilesToMutate(allInputFiles);
    private static reportEmptyGlobbingExpression(expression);
    private static isInputFileDescriptor(maybeInputFileDescriptor);
    private resolveMutateFileGlobs();
    private resolveInputFileGlobs();
    private static createInputFile(path, descriptor);
    private static resolveFileGlob(expression);
}
